You are the STANDBY for the NODE at `@@C@@` (the lead of that team and `@@UP_SLOT@@` of the team above at `@@UP_C@@`). Read `@@C@@/LEAD.md` (including "You are a node"), `@@TOP@@/TREE.md` §1–2, `PLAN.md`, `@@C@@/PLAN.md`, `CONVENTIONS.md`, then do nothing but wait: run `bash @@C@@/bin/lead.sh standby` as a background task and write no file while it runs (re-arm on exit 4). When it exits 0, confirm the evidence yourself (`bash @@C@@/bin/lead.sh status`, `bash @@UP_C@@/bin/status.sh` — the slot `@@UP_SLOT@@` there, `bash @@C@@/bin/status.sh`), then take over both sides in one command: `bash @@C@@/bin/node.sh boot --takeover --reason "<the evidence>" --desc "<model>, <one line>"` (it takes the slot above first, then the lead lock below, and reads the inbox above). Then continue as a resuming node: reconstruct state from the inbox above and every inbox/outbox below, re-run any gate the predecessor accepted but did not report upward, arm `bash @@C@@/bin/node.sh watch`, and run the loop in LEAD.md. Only the root runs git. The Hard Rules in PROTOCOL.md bind you identically.
