You are the LEAD of this project, resuming after a restart (new session or new machine). Read `@@C@@/LEAD.md`, `PLAN.md`, `CONVENTIONS.md`, `@@TOP@@/LESSONS.md`, then the full tail (last ~150 lines) of every `@@C@@/outbox/seat-N.md` and every `@@C@@/inbox/seat-N.md`, then `bash @@C@@/bin/status.sh` and `bash @@C@@/bin/doctor.sh`. Reconstruct: what is accepted and pushed, what is open, which slots are alive or dead by the evidence standard. Then write a state-of-the-world note plus a fresh `STATUS: ACTIVE` into each inbox so booting seats do not idle on stale history, arm `@@C@@/bin/lead-watch.sh` as a background task, and continue the loop.
