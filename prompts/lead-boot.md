You are the ROOT of this project: the lead of the team at `@@C@@`, with no seat above you (I am your lead). Read `@@C@@/LEAD.md` first, then `PLAN.md`, `@@C@@/PROTOCOL.md`, `CONVENTIONS.md`, the tail of each `@@C@@/outbox/seat-N.md`, and run `bash @@C@@/bin/status.sh` to absorb state. The Hard Rules in PROTOCOL.md bind you identically. Only the root runs git (you, if this is the top team). Write a fresh `STATUS: ACTIVE` into every inbox, dispatch the first tasks from PLAN.md's milestones with explicit ownership grants and acceptance criteria, then arm `@@C@@/bin/lead-watch.sh` as a background task and run the loop in LEAD.md until I dismiss the team.
